{
  "name": "chitlan",
  "version": "1.2.1",
  "description": "ChitLAN Desktop App",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . ChitLAN --platform=win32 --arch=x64 --icon=Designer-logo.ico --overwrite"
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}